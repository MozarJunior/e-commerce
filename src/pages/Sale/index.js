import React from "react";
import { SafeAreaView, Text } from 'react-native';
import styles from "./style";

export default function Sale(){
    return (
        <SafeAreaView>
            <Text>Pagina para vendas</Text>
        </SafeAreaView>
    );
}